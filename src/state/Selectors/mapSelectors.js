export const selectCenter = (state) => state.map.center;
export const selectZoom = (state) => state.map.zoom;
export const selectMarkers = (state) => state.map.markers;
export const selectMarkerInfo = (state) => state.map.markerInfo;
export const selectFetchMarkers = (state) => state.map.fetchMarkers;
